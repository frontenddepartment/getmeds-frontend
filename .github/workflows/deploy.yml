name: Deploy to cPanel

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
        env:
          # Non-sensitive values read from Environment variables (vars.*).
          # If left unset, vite.config.js/scripts fall back to their
          # development defaults, so the build still succeeds either way.
          VITE_DEPLOYMENT: production
          VITE_CHATBOT_API_URL: ${{ vars.VITE_CHATBOT_API_URL }}
          VITE_SPREADSHEET_API_URL: ${{ vars.VITE_SPREADSHEET_API_URL }}
          VITE_SANITY_PROJECT_ID: ${{ vars.VITE_SANITY_PROJECT_ID }}
          VITE_SANITY_DATASET: ${{ vars.VITE_SANITY_DATASET }}
          VITE_SANITY_API_VERSION: ${{ vars.VITE_SANITY_API_VERSION }}
          VITE_WORDPRESS_API_BASE: ${{ vars.VITE_WORDPRESS_API_BASE }}
          VITE_WORDPRESS_API_ROOT: ${{ vars.VITE_WORDPRESS_API_ROOT }}
          VITE_ALLOWED_CORS_ORIGIN: ${{ vars.VITE_ALLOWED_CORS_ORIGIN }}
          # Sensitive — must live in Environment SECRETS, not variables.
          SANITY_WRITE_TOKEN: ${{ secrets.SANITY_WRITE_TOKEN }}

      - name: Deploy to cPanel via FTPS
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ vars.FTP_SERVER }}
          username: ${{ vars.FTP_USERNAME }}
          # Sensitive — must live in Environment SECRETS, not variables.
          password: ${{ secrets.FTP_PASSWORD }}
          server-dir: ${{ vars.FTP_SERVER_DIR }}
          local-dir: ./dist/
          exclude: |
            **/.git*
            **/.git*/**
          # This action includes dotfiles (like .htaccess) by default —
          # do not add .htaccess to the exclude list above.
