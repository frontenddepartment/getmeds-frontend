import React from 'react'
import ReactDOM from 'react-dom/client'
import PrescriptionPolicy from '../pages/prescription-policy'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PrescriptionPolicy />
  </React.StrictMode>
)
